@model IEnumerable<MeetingVL.Models.Evaluate>

@{
    ViewBag.Title = "Index";
    Layout = null;
}
@if (Model.Count() != 0)
{
    foreach (var item in Model)
    {
        <a class="Info p-0" href="@Url.Action("Details", "Evaluates", new { id = item.ID })">
            <i class="fa ft-clipboard font-medium-3 mr-2"></i> 
        </a>
    }
}
else
{
    
}


